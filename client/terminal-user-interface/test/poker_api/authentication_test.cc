#include "catch2/catch.hpp"
#include "__test/test_helper.hh"
#include "poker_api/authentication.hh"

using namespace terminal_user_interface;
using namespace terminal_user_interface::poker_api;

TEST_CASE("Can properly authenticate a user to the poker API", "[poker_api]") {
    // TODO: tests are to be implemented in the future (I don't have time right now)
    
    SECTION("Registering a new user") {
        SECTION("Successfully registers a new user") {

        }
    }
}

